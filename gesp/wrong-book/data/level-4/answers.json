{
  "schemaVersion": 1,
  "level": 4,
  "updatedAt": "2026-08-31",
  "answers": [
    {"id": "l4-202607-s1", "answer": "C", "explanation": ["稳定排序要求相等元素排序后相对位置不变。选择排序每轮把最小值交换到前面，可能破坏相等元素的相对顺序，因此选择排序一般不稳定，错误说法是 C。"]},
    {"id": "l4-202607-s2", "answer": "C", "explanation": ["当 y 为 0 时，程序抛出字符串常量 Zero Error，类型可以被 catch (const char* msg) 捕获。因此输出 Caught: Zero Error，选 C。"]},
    {"id": "l4-202607-s3", "answer": "C", "explanation": ["代码每一轮从未排序部分中找出最小元素的位置 minPos，再与当前位置交换。这是选择排序的典型过程，所以选 C。"]},
    {"id": "l4-202607-s4", "answer": "A", "explanation": ["文件中读入后，s 为 Level，长度为 5，v 为 10。因此输出 s.length() * v 的值，即 5 × 10 = 50，选 A。"]},
    {"id": "l4-202607-s5", "answer": "C", "explanation": ["数组共有 5 × 4 = 20 个 int，每个 int 占 4 字节，总大小为 20 × 4 = 80 字节。80 的十六进制是 0x50，所以 &map + 1 指向整个数组之后的位置，即 0x2000 + 0x50 = 0x2050，选 C。"]},
    {"id": "l4-202607-t1", "answer": "false", "explanation": ["引用一旦初始化后，就不能再重新绑定到其他变量。对引用赋值只是修改它所绑定变量的值，不是改变绑定对象，所以判断为错误。"]},
    {"id": "l4-202607-t2", "answer": "false", "explanation": ["即使寻找最小值时遇到相等元素不更新位置，最后的交换操作仍可能把前面的相等元素换到后面。所以选择排序一般不是稳定排序，判断为错误。"]},
    {"id": "l4-202607-t3", "answer": "true", "explanation": ["二维数组按行存储，编译器必须知道第二维大小，才能计算元素位置。初始化时第一维可以由初值推断，但第二维不能省略，所以判断为正确。"]},
    {"id": "l4-202607-t4", "answer": "true", "explanation": ["cout.rdbuf(fout.rdbuf()) 会把 cout 的缓冲区切换到文件输出流。随后输出的 GESP Exam 会写入文件，最后再恢复缓冲区，所以判断为正确。"]},
    {"id": "l4-202607-t5", "answer": "false", "explanation": ["数组名可以在很多表达式中退化为指针，但数组名本身不是可以修改的变量。不能对数组名执行自增操作，因此代码无法通过编译，判断为错误。"]},
    {"id": "l4-20260725-s1", "answer": "B", "explanation": ["`Point p = {1,2};` 使用列表初始化，按成员声明顺序把 x 初始化为 1、y 初始化为 2，是合法的结构体初始化。A 中 `(1,2)` 是逗号表达式，C 把指针赋给对象，D 不是合法的 C++ 语法，所以选 B。"]},
    {"id": "l4-20260725-s2", "answer": "B", "explanation": ["`divi(10, 0)` 执行 `throw 0`，抛出的异常类型是 `int`。第一个 `catch (const char*)` 类型不匹配而跳过，随后被 `catch (int)` 捕获并输出 B，所以选 B。"]},
    {"id": "l4-20260725-s3", "answer": "A", "explanation": ["排行榜按分数从高到低排列。当前玩家分数增加后，只要 `i > 0` 且其分数高于前一个玩家，就要把前一个元素向后搬移。`i > 0` 同时避免访问 `players[-1]`，所以选 A。"]},
    {"id": "l4-20260725-t1", "answer": "false", "explanation": ["程序虽然创建了指向 `data.txt` 的文件输出流 `fout`，但实际使用的是 `cout << \"Hello\"`，内容会输出到屏幕，而不会写入文件。关闭 `fout` 也不会补写内容，所以判断为错误。"]},
    {"id": "l4-20260725-t2", "answer": "false", "explanation": ["相同的大 O 时间复杂度不代表实际表现相同。插入排序对基本有序的数据可接近 O(n)，而选择排序仍需进行大量比较；两者的交换次数、稳定性和适用场景也不同，因此不能在任何场景下互相替代。"]},
    {"id": "l4-20260803-s1", "answer": "B", "explanation": ["三重循环枚举三维数组的全部元素，只在 i+j+k 为偶数时累加。满足条件的值是 1、4、5、8、9、12，它们的和为 39，所以选 B。"]},
    {"id": "l4-20260803-s2", "answer": "B", "explanation": ["第 i 轮要在尚未进入前 k 名的区间 [i+1, n) 中寻找最高分，因此 j 应从 i+1 开始，并在 j<n 时继续。完整写法是 `int j = i + 1; j < n; j++`，所以选 B。"]},
    {"id": "l4-20260803-s3", "answer": "D", "explanation": ["`fin >> s1` 读到 Happy 后停在换行符之前，换行符仍留在输入流中。紧接着调用 `getline(fin, s2)` 时会立即读到这个换行符，因此 s2 是空字符串，最终输出 `Happy|`，所以选 D。"]},
    {"id": "l4-20260803-t1", "answer": "false", "explanation": ["调用 `calculate(5)` 时，既可以匹配带默认参数的 `calculate(int, int)`，也可以匹配重载函数 `calculate(int)`。两个候选同样合适，形成二义性，程序不能通过编译，所以判断为错误。"]},
    {"id": "l4-20260803-t2", "answer": "true", "explanation": ["文件中依次写入 10、20、30、40，空格和换行都能被格式化整数输入自动跳过。四个整数读取成功后相加得到 100，所以判断为正确。"]},
    {"id": "l4-20260803-s4", "answer": "C", "explanation": ["`p = arr` 后，p 指向数组第一个元素。执行 `p++` 后，p 指向下一个 int，也就是 `arr[1]`，解引用得到 2，所以选 C。"]},
    {"id": "l4-20260803-s5", "answer": "C", "explanation": ["p 是 int 指针，执行 `p++` 会向后移动一个 int 的大小。按题目所用的 4 字节 int 计算，地址从 `0x6ffe14` 增加 4，得到 `0x6ffe18`，所以选 C。"]},
    {"id": "l4-20260803-s6", "answer": "A", "explanation": ["`alpha[0]` 初始化为 ASCII 码 65，即字母 A。循环从下标 1 开始，每一项应在前一项的字符编码基础上加 1，才能依次得到 B 到 Z，因此应填写 `alpha[i] = alpha[i-1] + 1;`，选 A。"]},
    {"id": "l4-20260803-s7", "answer": "A", "explanation": ["`operator<<` 对整数执行的是格式化文本输出，整数 6 写入文件时只占一个字符，并不是把 4 字节的 int 内存直接写入。循环共写入 8 个字符 A 和 2 个字符 6，所以文件共有 10 个字节，选 A。"]},
    {"id": "l4-20260803-s8", "answer": "B", "explanation": ["选项 B 从 i=2 开始检查，因此当 (1,2) 是 N 的相邻因数对时会直接漏掉这一对。其他写法从 1 开始检查，或用 i-1 与 i 表示相邻整数；按题目给出的判断逻辑，找不到所有因数对的是 B。"]},
    {"id": "l4-20260803-t3", "answer": "true", "explanation": ["递归求 n 的阶乘会从 n 逐层递归到 1，共进行与 n 成正比的递归调用；每层只做常数次运算，因此时间复杂度是 O(n)，判断为正确。"]},
    {"id": "l4-20260803-t4", "answer": "false", "explanation": ["`filein` 是输入文件流，并且打开了 `1.txt`，不能用于写入；真正用于写入的 `fileout` 没有打开任何文件。执行 `fileout << \"hello\"` 不会把内容写入 `1.txt`，所以判断为错误。"]},
    {"id": "l4-20260809-s1", "answer": "B", "explanation": ["插入排序在数组已经有序时，每个元素只需与前一个元素比较，整体只需一趟线性扫描，最好时间复杂度为 O(n)；数组完全逆序时，每个元素都要向前移动很多次，最坏时间复杂度为 O(n^2)，所以选 B。"]},
    {"id": "l4-20260809-t1", "answer": "false", "explanation": ["指针变量必须用地址初始化。`int *p = a;` 右侧的 `a` 是整数值 5，不是地址；正确写法应为 `int *p = &a;`，所以判断为错误。"]},
    {"id": "l4-20260809-t2", "answer": "false", "explanation": ["在 `void f()` 中，局部变量 `x` 会遮蔽全局变量 `x`。初始化表达式 `int x = x + 1;` 右侧的 `x` 指向这个尚未初始化的局部变量，读取其值会产生未定义行为，不能保证输出 11，所以判断为错误。"]},
    {"id": "l4-20260809-t3", "answer": "true", "explanation": ["定义结构体后，使用 `new Student[20]` 动态分配 20 个 `Student` 对象是合法的，返回值类型为 `Student*`，可以赋给指针 `students`，所以判断为正确。"]},
    {"id": "l4-20260809-t4", "answer": "false", "explanation": ["一轮冒泡排序从左到右比较并交换相邻逆序元素，把最大值移到末尾。数组 `{4, 1, 3, 1, 5, 2}` 执行一轮后应为 `{1, 3, 1, 4, 2, 5}`，题目给出的结果不正确，所以判断为错误。"]},
    {"id": "l4-20260816-s1", "answer": "C", "explanation": ["冒泡排序和插入排序都可以保持相等元素的相对顺序，属于稳定排序；选择排序可能通过交换改变相等元素的相对顺序，通常是不稳定的，所以选 C。"]},
    {"id": "l4-20260816-s2", "answer": "B", "explanation": ["`int *p` 表示 p 是指向 int 类型的指针，`nullptr` 表示它当前不指向有效对象。空指针地址不是随机地址，解引用空指针也不是编译错误，而是未定义行为，所以正确的是 B。"]},
    {"id": "l4-20260816-s3", "answer": "C", "explanation": ["二维数组按行连续存储。`array[1][2]` 是第 1 行最后一个元素，下一位置是 `array[2][0]`，再下一位置才是 `array[2][1]`，两者相差 2 个 int。按每个 int 占 4 字节计算，相差 8 字节，所以选 C。"]},
    {"id": "l4-20260816-t1", "answer": "false", "explanation": ["函数可以在没有 `try` 子句的情况下被调用；如果异常没有在当前层捕获，会沿调用栈向上层传播。只有需要在当前层处理异常时才使用 `try-catch`，所以判断为错误。"]},
    {"id": "l4-20260816-t2", "answer": "false", "explanation": ["`char s[100][10]` 表示 100 行、每行 10 个 char，不是 10 个长度为 99 的字符串。应定义为 `char s[10][100]`，每行预留 100 个 char（含字符串结束符），所以判断为错误。"]},
    {"id": "l4-20260816-t3", "answer": "true", "explanation": ["`>=` 是大于等于关系运算符，`>>=` 是右移并赋值的复合运算符，二者都是 C++ 语言合法的运算符，所以判断为正确。"]},
    {"id": "l4-20260816-t4", "answer": "true", "explanation": ["文件重定向后，`cout` 的输出可能被写入文件或其他输出目标，而不一定显示在屏幕上。仅从程序代码无法确定最终是否输出到屏幕，所以判断为正确。"]},
    {"id": "l4-20260823-s1", "answer": "D", "explanation": ["静态数组 `int arr[3][4]` 通常在栈上分配，离开作用域时会自动释放；动态数组 `new int[12]` 在堆上分配，必须使用 `delete[] arr` 手动释放，否则会造成内存泄漏。两种方式都分配了连续的 12 个 `int` 空间，因此错误说法是 D。"]},
    {"id": "l4-20260823-s2", "answer": "C", "explanation": ["插入排序在数据基本有序时效率较高，因为每次插入通常只需移动少量元素，最好时间复杂度可达 O(n)。数据量大且乱序严重时，其 O(n^2) 性能较差；若主要追求较少交换次数，选择排序通常更合适，所以选 C。"]},
    {"id": "l4-20260823-s3", "answer": "D", "explanation": ["每次计算 `fibonacci(n)` 都会继续调用 `fibonacci(n - 1)` 和 `fibonacci(n - 2)`，形成包含大量重复计算的递归树。递推关系近似为 T(n) = T(n - 1) + T(n - 2) + O(1)，调用次数呈指数增长，所以时间复杂度可记为 O(2^n)，选 D。"]}
    ,{"id": "l4-20260829-s1", "answer": "D", "explanation": ["`GESP.txt`、`../data/GESP.txt` 和 `./data/GESP.txt` 都是相对路径，分别表示当前工作目录、上一级目录下的 data 目录和当前目录下的 data 目录中的文件。只有绝对路径会从根目录或盘符开始，因此错误说法是 D。"]}
    ,{"id": "l4-20260829-s2", "answer": "A", "explanation": ["第一遍冒泡排序从左到右比较相邻元素，把较大的元素逐步交换到末尾。依次比较后，最大值 97 被移到最后，序列变为 `{45, 23, 1, 10, 66, 52, 88, 5, 33, 97}`，所以选 A。"]}
    ,{"id": "l4-20260829-s3", "answer": "D", "explanation": ["插入排序在 `a[j] > key` 时，需要把当前较大的元素向右搬移一格，为 key 腾出位置，因此横线处应为 `a[j+1] = a[j];`，选 D。"]}
    ,{"id": "l4-20260829-t1", "answer": "true", "explanation": ["引用声明时必须立即绑定到一个对象，`int& ra;` 没有初始化是非法的；而且 `ra = &a` 右侧是 `int*`，也不能赋给 `int&`。因此代码不能正确执行，判断为正确。"]}
    ,{"id": "l4-20260829-t2", "answer": "true", "explanation": ["引用可以看作已经绑定且不能改指向的别名，其底层通常通过指针常量实现：引用一旦初始化就不能再绑定到其他对象。因此“引用是一个指针常量”在本题语境下判断为正确。"]}
    ,{"id": "l4-20260829-t3", "answer": "true", "explanation": ["`p` 被初始化为空指针常量 `NULL`，通过 `cout` 输出该指针时表示空地址，题目环境下输出为 0，因此判断为正确。"]}
    ,{"id": "l4-20260831-s1", "answer": "C", "explanation": ["`Person p = {\"Yang\", 10};` 按结构体成员声明顺序进行聚合初始化，分别把 `name` 和 `age` 初始化为 `\"Yang\"` 和 `10`。A 需要相应构造函数，B 的成员声明使用了错误的逗号，D 把 `new` 返回的指针赋给结构体对象，因此选 C。"]}
    ,{"id": "l4-20260831-s2", "answer": "C", "explanation": ["选项 C 虽然打开了 `outFile(\"log.txt\")`，但实际仍使用 `cout` 输出，既没有向 `outFile` 写入，也没有把 `cout` 的缓冲区重定向到文件，因此不能把内容写入 `log.txt`。A 使用 `freopen`，B 直接写入 `ofstream`，D 使用 `rdbuf` 重定向，均可实现题意，所以选 C。"]}
    ,{"id": "l4-20260831-t1", "answer": "true", "explanation": ["异常抛出后，如果沿调用栈一直没有匹配的处理程序，C++ 运行时会调用 `std::terminate()` 终止程序，因此判断为正确。"]}
  ]
}
